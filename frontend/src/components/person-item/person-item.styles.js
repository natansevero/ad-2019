import styled from 'styled-components';
import { ListGroup } from 'react-bootstrap';

export const PersonItemContainer = styled(ListGroup.Item)`
    &:hover {
        background-color: #eaeaea;
    }
`